[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "optivest-backend"
version = "0.1.0"
description = "Backend API for OptiVest"
requires-python = ">=3.11"

authors = [
    {name = "Arceus"}
]